name: static_map_service
description: A Dart package for generating static map images url from Google Maps, Apple Snapshots.
version: 0.0.1
homepage: https://github.com/koji-1009/static_map_service

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0
  test: ^1.0.0

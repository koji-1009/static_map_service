name: static_map_service
description: A type-safe Dart package for generating static map image URLs from Google Maps, Apple Maps, and Mapbox.
version: 1.0.0
repository: https://github.com/koji-1009/static_map_service
issue_tracker: https://github.com/koji-1009/static_map_service/issues

topics:
  - map
  - static-maps
  - google-maps
  - apple-maps
  - mapbox

environment:
  sdk: '>=3.8.0 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.27.0
  coverage: ^1.14.0
